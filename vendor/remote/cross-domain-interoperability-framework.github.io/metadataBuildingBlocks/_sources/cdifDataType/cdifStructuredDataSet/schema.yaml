---
$schema: https://json-schema.org/draft/2020-12/schema
title: Structured DataSet
description: Dataset-level marker for a structured (hierarchical) dataset such as XML or
  JSON. A subclass of cdi:PhysicalDataSet, carried on a schema:DataDownload distribution
  dual-typed cdi:StructuredDataSet. DDI-CDI StructuredDataSet. It has no distinct
  tabular-layout attributes; fields are located with cdifLocatorMapping values of
  cdif:hasPhysicalMapping (rather than column-index/text mappings). This is an attribute
  mixin (no @type of its own; the token is carried by the distribution).
type: object
properties:
  cdif:encoding:
    type: string
    description: Character encoding / serialization of the structured document (e.g., utf-8,
      application/json). CDIF simplification (plain charset string) of DDI-CDI
      PhysicalDataSet.encoding, whose model type is ControlledVocabularyEntry. The cdi/cdif
      namespace policy does not allow a ControlledVocabularyEntry as a plain string in the cdi
      namespace, so this property is in the cdif namespace.
