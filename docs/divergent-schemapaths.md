# Divergent schema paths

Metadata Items mapped **differently** in different TAPP sidecars. Generated by
`tools/mark_shared_mappings.py --divergent-report`; regenerate rather than hand-edit.

Paths are shown canonicalised (`normalize_schema_paths.mechanical`), so `$.` shorthand and
stray whitespace are not counted as differences. An item is divergent only when the SET of
paths differs between sidecars — an item that is dual-homed everywhere is agreement, not
divergence.

Variants are ordered most-used first, so the majority mapping is at the top of each entry
and the outlier is what to look at. The tier columns are worth checking before treating a
difference as a defect: if two sidecars assign different Protocol/Analysis tiers, the matrix
requires different paths and the divergence is correct.

**48 divergent items** across 10 sidecars.

---

## Accelerating Voltage

- **`$MethodDefinition.ada:acceleratingVoltage`**
    - `Lab-XCT_TAPP_v8` — tiers: Basic / Editable
    - `SEM_Composition_TAPP_v4` — tiers: Basic / Editable
    - `SEM_Imaging_TAPP_v4` — tiers: Basic / Editable
    - `SEM_TAPP_v4` — tiers: Basic / Editable
- **`$MethodDefinition.schema:instrument[schema:additionalType='EPMA'].ada:acceleratingVoltage`**
    - `EPMA_TAPP_v7` — tiers: Basic / Editable
- **`$MethodDefinition.schema:instrument[schema:additionalType='SEM'].schema:additionalProperty[schema:name='acceleratingVoltage'].schema:value`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Editable

## BSE Detector Type

- **`$MethodDefinition.ada:bseDetectorType`**
    - `SEM_Imaging_TAPP_v4` — tiers: Basic / Read-Only
    - `SEM_TAPP_v4` — tiers: Basic / Read-Only
- **`$MethodDefinition.schema:instrument[schema:additionalType='SEM'].schema:hasPart[schema:additionalType='BSEDetector'].schema:name`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Read-Only

## Background Correction Method

- **`$MethodDefinition.ada:backgroundCorrectionMethod`**
    - `SEM_Composition_TAPP_v4` — tiers: Basic / Editable
    - `SEM_TAPP_v4` — tiers: Basic / Editable
- **`$Dataset.prov:wasGeneratedBy.schema:actionProcess.schema:step[schema:name='dataReduction'].schema:additionalProperty[schema:name='Background Correction Method'].schema:value`**
    - `EPMA_TAPP_v7` — tiers: Basic / Editable

## Background Counting Time  *(analyte template)*

> Some sidecars model this as a per-analyte COLUMN and others as a per-session property. That is a real modelling difference, not necessarily an inconsistency — decide which the measurement actually is. An analyte column has no analysis-tier path: its values are the `ada:defaultAnalytes` rows, surfacing on the dataset as `schema:variableMeasured`.

- **`$MethodDefinition.ada:backgroundCountingTime`**
    - `SEM_Composition_TAPP_v4` — tiers: Basic / Editable
    - `SEM_TAPP_v4` — tiers: Basic / Editable
- **`$MethodDefinition.ada:analyteTemplate.ada:analyteColumns[]`**
    - `EPMA_TAPP_v7` — tiers: Basic / Editable

## Background Position(s)  *(analyte template)*

> Some sidecars model this as a per-analyte COLUMN and others as a per-session property. That is a real modelling difference, not necessarily an inconsistency — decide which the measurement actually is. An analyte column has no analysis-tier path: its values are the `ada:defaultAnalytes` rows, surfacing on the dataset as `schema:variableMeasured`.

- **`$Dataset.schema:additionalProperty[schema:name='Background Position s'].schema:value`**
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Editable
    - `SEM_TAPP_v4` — tiers: Advanced / Editable
- **`$MethodDefinition.schema:additionalProperty[schema:name='Background Position s'].schema:defaultValue`**
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Editable
    - `SEM_TAPP_v4` — tiers: Advanced / Editable
- **`$MethodDefinition.ada:analyteTemplate.ada:analyteColumns[]`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Editable
- **`$MethodDefinition.ada:analyteTemplate.ada:analyteColumns[].schema:defaultValue`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Editable

## Beam Current  *(analyte template)*

> Some sidecars model this as a per-analyte COLUMN and others as a per-session property. That is a real modelling difference, not necessarily an inconsistency — decide which the measurement actually is. An analyte column has no analysis-tier path: its values are the `ada:defaultAnalytes` rows, surfacing on the dataset as `schema:variableMeasured`.

- **`$MethodDefinition.ada:beamCurrent`**
    - `SEM_Composition_TAPP_v4` — tiers: Basic / Editable
    - `SEM_Imaging_TAPP_v4` — tiers: Basic / Editable
    - `SEM_TAPP_v4` — tiers: Basic / Editable
- **`$MethodDefinition.ada:analyteTemplate.ada:analyteColumns[]`**
    - `EPMA_TAPP_v7` — tiers: Basic / Editable
- **`$MethodDefinition.schema:instrument[schema:additionalType='SEM'].schema:additionalProperty[schema:name='beamCurrent'].schema:value`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Editable

## Beam Diameter

- **`$MethodDefinition.ada:beamDiameter`**
    - `SEM_Composition_TAPP_v4` — tiers: Basic / Editable
    - `SEM_TAPP_v4` — tiers: Basic / Editable
- **`$MethodDefinition.schema:instrument[schema:additionalType='EPMA'].ada:beamDiameter`**
    - `EPMA_TAPP_v7` — tiers: Basic / Editable

## Beam Mode

- **`$MethodDefinition.ada:beamMode`**
    - `SEM_Composition_TAPP_v4` — tiers: Basic / Read-Only
    - `SEM_TAPP_v4` — tiers: Basic / Read-Only
- **`$MethodDefinition.schema:instrument[schema:additionalType='EPMA'].ada:beamMode`**
    - `EPMA_TAPP_v7` — tiers: Basic / Read-Only

## Beam Raster Dimensions

- **`$Dataset.schema:additionalProperty[schema:name='Beam Raster Dimensions'].schema:value`**
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Editable
    - `SEM_TAPP_v4` — tiers: Advanced / Editable
- **`$MethodDefinition.schema:additionalProperty[schema:name='Beam Raster Dimensions'].schema:defaultValue`**
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Editable
    - `SEM_TAPP_v4` — tiers: Advanced / Editable
- **`$Dataset.prov:wasGeneratedBy.schema:actionProcess.schema:step[schema:name='dataReduction'].schema:additionalProperty[schema:name='Beam Raster Dimensions'].schema:value`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Editable
- **`$MethodDefinition..schema:actionProcess.schema:step[schema:name='dataReduction'].schema:additionalProperty[schema:name='Beam Raster Dimensions'].schema:defaultValue`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Editable

## Blank Correction

- **`$Dataset.schema:additionalProperty[schema:name='Blank Correction'].schema:value`**
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Editable
    - `SEM_TAPP_v4` — tiers: Advanced / Editable
- **`$MethodDefinition.schema:additionalProperty[schema:name='Blank Correction'].schema:defaultValue`**
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Editable
    - `SEM_TAPP_v4` — tiers: Advanced / Editable
- **`$Dataset.prov:wasGeneratedBy.schema:actionProcess.schema:step[schema:name='dataReduction'].schema:additionalProperty[schema:name='Blank Correction'].schema:value`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Editable
- **`$MethodDefinition..schema:actionProcess.schema:step[schema:name='dataReduction'].schema:additionalProperty[schema:name='Blank Correction'].schema:defaultValue`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Editable

## CL Grating

- **`$Dataset.ada:clWavelengthRange`**
    - `SEM_Imaging_TAPP_v4` — tiers: Advanced / Read-Only
- **`$MethodDefinition.schema:additionalProperty[schema:name='CL Grating'].schema:value`**
    - `SEM_TAPP_v4` — tiers: Advanced / Read-Only

## Coarse Milling Conditions

- **`$MethodDefinition.ada:coarseMillingConditions`**
    - `SEM_TAPP_v4` — tiers: Basic / Editable
- **`$MethodDefinition.schema:actionProcess.schema:step[schema:name='ionMilling'].schema:additionalProperty[schema:name='Coarse Milling Conditions'].schema:value`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Editable

## Coupled Technique(s)

- **`$Dataset.schema:relatedLink[schema:linkRelationship='coupledTechnique'].schema:target.schema:name`**
    - `LA-ICPMS_Geochron_Horstwood_TAPP_v6` — tiers: Advanced / Editable
    - `LA-Q_SF-ICPMS_TAPP_v3` — tiers: Advanced / Editable
    - `Lab-XCT_TAPP_v8` — tiers: Advanced / Editable
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Editable
    - `SEM_Imaging_TAPP_v4` — tiers: Advanced / Editable
    - `SEM_TAPP_v4` — tiers: Advanced / Editable
    - `Solution_Q-ICP-MS_TAPP_v5` — tiers: Advanced / Editable
    - `Solution_SF-ICP-MS_TAPP_v5` — tiers: Advanced / Editable
- **`$MethodDefinition.schema:relatedLink[schema:linkRelationship='coupledTechnique'].schema:target.schema:name`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Editable
    - `SEM_FIBSEM_TAPP_v4` — tiers: Advanced / Editable

## Diffracting Crystal  *(analyte template)*

> Some sidecars model this as a per-analyte COLUMN and others as a per-session property. That is a real modelling difference, not necessarily an inconsistency — decide which the measurement actually is. An analyte column has no analysis-tier path: its values are the `ada:defaultAnalytes` rows, surfacing on the dataset as `schema:variableMeasured`.

- **`$MethodDefinition.ada:diffractingCrystal`**
    - `SEM_Composition_TAPP_v4` — tiers: Basic / Read-Only
    - `SEM_TAPP_v4` — tiers: Basic / Read-Only
- **`$MethodDefinition.ada:analyteTemplate.ada:analyteColumns[]`**
    - `EPMA_TAPP_v7` — tiers: Basic / Read-Only

## Dwell Time per Pixel  *(analyte template)*

> Some sidecars model this as a per-analyte COLUMN and others as a per-session property. That is a real modelling difference, not necessarily an inconsistency — decide which the measurement actually is. An analyte column has no analysis-tier path: its values are the `ada:defaultAnalytes` rows, surfacing on the dataset as `schema:variableMeasured`.

- **`$MethodDefinition.ada:dwellTimePerPixel`**
    - `SEM_Composition_TAPP_v4` — tiers: Basic / Editable
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Editable
    - `SEM_Imaging_TAPP_v4` — tiers: Basic / Editable
    - `SEM_TAPP_v4` — tiers: Basic / Editable
- **`$MethodDefinition.ada:analyteTemplate.ada:analyteColumns[]`**
    - `EPMA_TAPP_v7` — tiers: Basic / Editable

## EBSD Frame Time

- **`$Dataset.ada:frameTime`**
    - `SEM_Imaging_TAPP_v4` — tiers: Advanced / Editable
- **`$Dataset.schema:additionalProperty[schema:name='EBSD Frame Time'].schema:value`**
    - `SEM_TAPP_v4` — tiers: Advanced / Editable
- **`$MethodDefinition.schema:additionalProperty[schema:name='EBSD Frame Time'].schema:defaultValue`**
    - `SEM_TAPP_v4` — tiers: Advanced / Editable

## EBSD Indexing Method

- **`$Dataset.ada:indexingMethod`**
    - `SEM_Imaging_TAPP_v4` — tiers: Basic / Read-Only
- **`$MethodDefinition.ada:ebsdIndexingMethod`**
    - `SEM_TAPP_v4` — tiers: Basic / Read-Only

## EBSD Pattern Quality Threshold

- **`$Dataset.ada:patternQualityThreshold`**
    - `SEM_Imaging_TAPP_v4` — tiers: Advanced / Editable
- **`$Dataset.schema:additionalProperty[schema:name='EBSD Pattern Quality Threshold'].schema:value`**
    - `SEM_TAPP_v4` — tiers: Advanced / Editable
- **`$MethodDefinition.schema:additionalProperty[schema:name='EBSD Pattern Quality Threshold'].schema:defaultValue`**
    - `SEM_TAPP_v4` — tiers: Advanced / Editable

## EBSD Phase List

- **`$Dataset.ada:phaseList`**
    - `SEM_Imaging_TAPP_v4` — tiers: Basic / Editable
- **`$MethodDefinition.ada:ebsdPhaseList`**
    - `SEM_TAPP_v4` — tiers: Basic / Editable

## EBSD Step Size

- **`$Dataset.ada:stepSize`**
    - `SEM_Imaging_TAPP_v4` — tiers: Basic / Editable
- **`$MethodDefinition.ada:ebsdStepSize`**
    - `SEM_TAPP_v4` — tiers: Basic / Editable

## EDS Detector Configuration

- **`$MethodDefinition.schema:additionalProperty[schema:name='EDS Detector Configuration'].schema:value`**
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Read-Only
    - `SEM_TAPP_v4` — tiers: Advanced / Read-Only
- **`$MethodDefinition.schema:instrument[schema:additionalType='EPMA'].schema:additionalProperty[schema:name='EDS Detector Configuration'].schema:value`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Read-Only

## Electron Source

- **`$MethodDefinition.ada:electronSource`**
    - `SEM_Composition_TAPP_v4` — tiers: Basic / Read-Only
    - `SEM_Imaging_TAPP_v4` — tiers: Basic / Read-Only
    - `SEM_TAPP_v4` — tiers: Basic / Read-Only
- **`$MethodDefinition.schema:instrument[schema:additionalType='EPMA'].ada:electronSource`**
    - `EPMA_TAPP_v7` — tiers: Basic / Read-Only
- **`$MethodDefinition.schema:instrument[schema:additionalType='SEM'].schema:hasPart[schema:additionalType='Electron Source'].schema:name`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Read-Only

## Fine Polishing Conditions

- **`$MethodDefinition.ada:finePolishingConditions`**
    - `SEM_TAPP_v4` — tiers: Basic / Editable
- **`$MethodDefinition.schema:actionProcess.schema:step[schema:name='ionMilling'].schema:additionalProperty[schema:name='Fine Polishing Conditions'].schema:value`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Editable

## ICP Tuning

- **`$MethodDefinition.schema:additionalProperty[schema:name='ICP Tuning'].schema:defaultValue`**
    - `LA-ICPMS_Geochron_Horstwood_TAPP_v6` — tiers: Advanced / Editable
    - `LA-Q_SF-ICPMS_TAPP_v3` — tiers: Advanced / Editable
- **`$Dataset.schema:additionalProperty[schema:name='ICP Tuning'].schema:value`**
    - `LA-ICPMS_Geochron_Horstwood_TAPP_v6` — tiers: Advanced / Editable

## Instrument Manufacturer

- **`$MethodDefinition.ada:instrumentManufacturer`**
    - `SEM_Composition_TAPP_v4` — tiers: Basic / Read-Only
    - `SEM_Imaging_TAPP_v4` — tiers: Basic / Read-Only
    - `SEM_TAPP_v4` — tiers: Basic / Read-Only
- **`$MethodDefinition.schema:instrument[schema:additionalType='EPMA'].ada:instrumentManufacturer`**
    - `EPMA_TAPP_v7` — tiers: Basic / Read-Only
- **`$MethodDefinition.schema:instrument[schema:additionalType='SEM'].schema:manufacturer`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Read-Only

## Instrument Model

- **`$MethodDefinition.ada:instrumentModel`**
    - `SEM_Composition_TAPP_v4` — tiers: Basic / Read-Only
    - `SEM_Imaging_TAPP_v4` — tiers: Basic / Read-Only
    - `SEM_TAPP_v4` — tiers: Basic / Read-Only
- **`$MethodDefinition.schema:instrument[schema:additionalType='EPMA'].ada:instrumentModel`**
    - `EPMA_TAPP_v7` — tiers: Basic / Read-Only
- **`$MethodDefinition.schema:instrument[schema:additionalType='SEM'].schema:model`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Read-Only

## Instrument Variant

- **`$MethodDefinition.ada:instrumentVariant`**
    - `SEM_Composition_TAPP_v4` — tiers: Basic / Read-Only
    - `SEM_Imaging_TAPP_v4` — tiers: Basic / Read-Only
    - `SEM_TAPP_v4` — tiers: Basic / Read-Only
- **`$MethodDefinition.schema:instrument[schema:additionalType='SEM'].schema:description`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Read-Only

## Ion Beam Source

- **`$MethodDefinition.ada:ionBeamSource`**
    - `SEM_TAPP_v4` — tiers: Basic / Read-Only
- **`$MethodDefinition.schema:instrument[schema:additionalType='FIBSEM'].schema:hasPart[schema:additionalType='Ion Beam Source'].schema:name`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Read-Only

## Ion Milling Conditions (3D Tomography)

- **`$MethodDefinition.ada:ionMillingConditions`**
    - `SEM_TAPP_v4` — tiers: Basic / Editable
- **`$MethodDefinition.schema:actionProcess.schema:step[schema:name='ionMilling'].schema:description`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Editable

## Isotope Dilution Data Reduction Method

- **`$MethodDefinition.ada:isotopeDilutionDataReductionMethod`**
    - `LA-ICPMS_Geochron_Horstwood_TAPP_v6` — tiers: Advanced / Read-Only
    - `Solution_Q-ICP-MS_TAPP_v5` — tiers: Basic / Read-Only
    - `Solution_SF-ICP-MS_TAPP_v5` — tiers: Basic / Read-Only
- **`$Dataset.prov:wasGeneratedBy.schema:actionProcess.schema:step[schema:name='dataReduction'].schema:additionalProperty[schema:name='Isotope Dilution Data Reduction Method'].schema:value`**
    - `LA-Q_SF-ICPMS_TAPP_v3` — tiers: Basic / Read-Only

    > Tier assignments differ between sidecars — the paths may be correctly different. Reconcile the tiers first.

## Lift-out Method

- **`$MethodDefinition.ada:liftOutMethod`**
    - `SEM_TAPP_v4` — tiers: Basic / Read-Only
- **`$MethodDefinition.schema:actionProcess.schema:step[schema:name='ionMilling'].schema:additionalProperty[schema:name='Lift Out Method'].schema:value`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Read-Only

## Normalization / Standards-Based Correction

- **`$MethodDefinition.schema:additionalProperty[schema:name='Normalization Standards Based Correction'].schema:defaultValue`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Editable
    - `LA-Q_SF-ICPMS_TAPP_v3` — tiers: Advanced / Editable
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Editable
    - `SEM_TAPP_v4` — tiers: Advanced / Editable
- **`$Dataset.schema:additionalProperty[schema:name='Normalization Standards Based Correction'].schema:value`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Editable
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Editable
    - `SEM_TAPP_v4` — tiers: Advanced / Editable

## Peak Counting Time  *(analyte template)*

> Some sidecars model this as a per-analyte COLUMN and others as a per-session property. That is a real modelling difference, not necessarily an inconsistency — decide which the measurement actually is. An analyte column has no analysis-tier path: its values are the `ada:defaultAnalytes` rows, surfacing on the dataset as `schema:variableMeasured`.

- **`$MethodDefinition.ada:peakCountingTime`**
    - `SEM_Composition_TAPP_v4` — tiers: Basic / Editable
    - `SEM_TAPP_v4` — tiers: Basic / Editable
- **`$MethodDefinition.ada:analyteTemplate.ada:analyteColumns[]`**
    - `EPMA_TAPP_v7` — tiers: Basic / Editable

## Proportional Counter / Detector  *(analyte template)*

> Some sidecars model this as a per-analyte COLUMN and others as a per-session property. That is a real modelling difference, not necessarily an inconsistency — decide which the measurement actually is. An analyte column has no analysis-tier path: its values are the `ada:defaultAnalytes` rows, surfacing on the dataset as `schema:variableMeasured`.

- **`$MethodDefinition.schema:additionalProperty[schema:name='Proportional Counter Detector'].schema:value`**
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Read-Only
    - `SEM_TAPP_v4` — tiers: Advanced / Read-Only
- **`$MethodDefinition.ada:analyteTemplate.ada:analyteColumns[]`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Read-Only

## Protective Coating Deposition

- **`$MethodDefinition.ada:protectiveCoatingDeposition`**
    - `SEM_TAPP_v4` — tiers: Basic / Editable
- **`$MethodDefinition.schema:actionProcess.schema:step[schema:name='samplePreparation'].schema:additionalProperty[schema:name='Protective Coating Deposition'].schema:value`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Editable

## SE Detector Type

- **`$MethodDefinition.ada:seDetectorType`**
    - `SEM_Imaging_TAPP_v4` — tiers: Basic / Read-Only
    - `SEM_TAPP_v4` — tiers: Basic / Read-Only
- **`$MethodDefinition.schema:instrument[schema:additionalType='SEM'].schema:hasPart[schema:additionalType='SEDetector'].schema:name`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Read-Only

## Sequence  *(analyte template)*

> Some sidecars model this as a per-analyte COLUMN and others as a per-session property. That is a real modelling difference, not necessarily an inconsistency — decide which the measurement actually is. An analyte column has no analysis-tier path: its values are the `ada:defaultAnalytes` rows, surfacing on the dataset as `schema:variableMeasured`.

- **`$MethodDefinition.schema:additionalProperty[schema:name='Sequence'].schema:value`**
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Read-Only
    - `SEM_TAPP_v4` — tiers: Advanced / Read-Only
- **`$MethodDefinition.ada:analyteTemplate.ada:analyteColumns[]`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Read-Only

## Signal Smoothing

- **`$MethodDefinition.schema:additionalProperty[schema:name='Signal Smoothing'].schema:defaultValue`**
    - `LA-ICPMS_Geochron_Horstwood_TAPP_v6` — tiers: Advanced / Editable
    - `LA-Q_SF-ICPMS_TAPP_v3` — tiers: Advanced / Editable
- **`$Dataset.schema:additionalProperty[schema:name='Signal Smoothing'].schema:value`**
    - `LA-ICPMS_Geochron_Horstwood_TAPP_v6` — tiers: Advanced / Editable

## Slice Thickness

- **`$MethodDefinition.ada:sliceThickness`**
    - `SEM_TAPP_v4` — tiers: Basic / Editable
- **`$MethodDefinition.schema:actionProcess.schema:step[schema:name='ionMilling'].schema:additionalProperty[schema:name='Slice Thickness'].schema:value`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Editable

## Spike / Outlier Filtering Approach

- **`$MethodDefinition.schema:additionalProperty[schema:name='Spike Outlier Filtering Approach'].schema:defaultValue`**
    - `LA-ICPMS_Geochron_Horstwood_TAPP_v6` — tiers: Advanced / Editable
    - `LA-Q_SF-ICPMS_TAPP_v3` — tiers: Advanced / Editable
- **`$Dataset.schema:additionalProperty[schema:name='Spike Outlier Filtering Approach'].schema:value`**
    - `LA-ICPMS_Geochron_Horstwood_TAPP_v6` — tiers: Advanced / Editable

## Step Size / Pixel Size

- **`$MethodDefinition.schema:additionalProperty[schema:name='Step Size Pixel Size'].schema:value`**
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Basic
    - `SEM_TAPP_v4` — tiers: Advanced / Basic
- **`$MethodDefinition.ada:stepSizePixelSize`**
    - `EPMA_TAPP_v7` — tiers: Basic / Editable

    > Tier assignments differ between sidecars — the paths may be correctly different. Reconcile the tiers first.

## Target Foil Thickness

- **`$MethodDefinition.ada:targetFoilThickness`**
    - `SEM_TAPP_v4` — tiers: Basic / Editable
- **`$MethodDefinition.schema:actionProcess.schema:step[schema:name='ionMilling'].schema:additionalProperty[schema:name='Target Foil Thickness'].schema:value`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Editable

## Time-Dependent Intensity Correction  *(analyte template)*

> Some sidecars model this as a per-analyte COLUMN and others as a per-session property. That is a real modelling difference, not necessarily an inconsistency — decide which the measurement actually is. An analyte column has no analysis-tier path: its values are the `ada:defaultAnalytes` rows, surfacing on the dataset as `schema:variableMeasured`.

- **`$Dataset.schema:additionalProperty[schema:name='Time Dependent Intensity Correction'].schema:value`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Editable
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Editable
    - `SEM_TAPP_v4` — tiers: Advanced / Editable
- **`$MethodDefinition.schema:additionalProperty[schema:name='Time Dependent Intensity Correction'].schema:defaultValue`**
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Editable
    - `SEM_TAPP_v4` — tiers: Advanced / Editable
- **`$MethodDefinition.ada:analyteTemplate.ada:analyteColumns[].schema:defaultValue`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Editable

## WDS PHA Setting  *(analyte template)*

> Some sidecars model this as a per-analyte COLUMN and others as a per-session property. That is a real modelling difference, not necessarily an inconsistency — decide which the measurement actually is. An analyte column has no analysis-tier path: its values are the `ada:defaultAnalytes` rows, surfacing on the dataset as `schema:variableMeasured`.

- **`$MethodDefinition.schema:additionalProperty[schema:name='WDS PHA Setting'].schema:value`**
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Read-Only
    - `SEM_TAPP_v4` — tiers: Advanced / Read-Only
- **`$MethodDefinition.ada:analyteTemplate.ada:analyteColumns[]`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Read-Only

## WDS Spectrometer Channel  *(analyte template)*

> Some sidecars model this as a per-analyte COLUMN and others as a per-session property. That is a real modelling difference, not necessarily an inconsistency — decide which the measurement actually is. An analyte column has no analysis-tier path: its values are the `ada:defaultAnalytes` rows, surfacing on the dataset as `schema:variableMeasured`.

- **`$MethodDefinition.schema:additionalProperty[schema:name='WDS Spectrometer Channel'].schema:value`**
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Read-Only
    - `SEM_TAPP_v4` — tiers: Advanced / Read-Only
- **`$MethodDefinition.ada:analyteTemplate.ada:analyteColumns[]`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Read-Only

## WDS Spectrometer Configuration

- **`$MethodDefinition.schema:additionalProperty[schema:name='WDS Spectrometer Configuration'].schema:value`**
    - `SEM_Composition_TAPP_v4` — tiers: Advanced / Read-Only
    - `SEM_TAPP_v4` — tiers: Advanced / Read-Only
- **`$MethodDefinition.schema:instrument[schema:additionalType='EPMA'].schema:additionalProperty[schema:name='WDS Spectrometer Configuration'].schema:value`**
    - `EPMA_TAPP_v7` — tiers: Advanced / Read-Only

## Working Distance

- **`$MethodDefinition.ada:workingDistance`**
    - `SEM_Composition_TAPP_v4` — tiers: Basic / Editable
    - `SEM_Imaging_TAPP_v4` — tiers: Basic / Editable
    - `SEM_TAPP_v4` — tiers: Basic / Editable
- **`$MethodDefinition.schema:instrument[schema:additionalType='SEM'].schema:additionalProperty[schema:name='workingDistance'].schema:value`**
    - `SEM_FIBSEM_TAPP_v4` — tiers: Basic / Editable

## X-ray Line  *(analyte template)*

> Some sidecars model this as a per-analyte COLUMN and others as a per-session property. That is a real modelling difference, not necessarily an inconsistency — decide which the measurement actually is. An analyte column has no analysis-tier path: its values are the `ada:defaultAnalytes` rows, surfacing on the dataset as `schema:variableMeasured`.

- **`$MethodDefinition.ada:xRayLine`**
    - `SEM_Composition_TAPP_v4` — tiers: Basic / Read-Only
    - `SEM_TAPP_v4` — tiers: Basic / Read-Only
- **`$MethodDefinition.ada:analyteTemplate.ada:analyteColumns[]`**
    - `EPMA_TAPP_v7` — tiers: Basic / Read-Only
