
# TAPP Composition Module: CalibrationFactor (Schema)

`ogch.BaseSchema.modules.calibrationFactor` *v0.1*

The shared CalibrationFactor block of the 2026-08-11 TAPP library, composed by 14 of the sixteen delivery tables. 1 owned fields over 0 schema paths, split into the procedure and analysis halves a TAPP schema and a technique detail compose respectively. A profile over existing tappDefinition/adaProduct properties, not a new vocabulary. Generated from the module CSV and its schema-path sidecar.

[*Status*](http://www.opengis.net/def/status): Under development

## Description

# TAPP Composition Module: CalibrationFactor

The shared **CalibrationFactor** block of the 2026-08-11 TAPP library, composed by **14 of the sixteen** delivery tables — 1 owned fields over 0 schema paths.

Generated by `tools/build_module_bb.py` from `Module_CalibrationFactor.csv` and its schema-path sidecar. Edit those, not the schema.

This is a **profile, not a vocabulary**: it asserts which properties a conforming procedure or analysis carries, all of them already defined by `tappDefinition` or `adaProduct`.

## The two `$defs`

| `$def` | composed into | properties | required |
|---|---|---|---|

Requiredness follows the TAPP tier matrix: a field Basic on a side is required there, Advanced is permitted, N/A is absent from that side entirely.

## Composing it

```yaml
allOf:
- $ref: ../../../BaseSchema/tappDefinition/schema.yaml
- $ref: ../../../BaseSchema/modules/calibrationFactor/schema.yaml#/$defs/ProcedureIdentification
- type: object
  properties: {}   # only what this technique itself owns
```

`allOf` composes constraints and cannot relax them: a technique composing this module accepts its required set in full.

## Examples

### CalibrationFactor published parameter (Param_Analysis_calibrationFactorAndDeterminationMethod)
A parameter the CalibrationFactor module publishes for the analysis side. The module places no root fields, so its parameters are what it contributes: a consuming TAPP unions this branch into its own additionalProperty anyOf.
#### json
```json
{
  "@id": "ada:parameter/module/CalibrationFactor/calibrationFactorAndDeterminationMethod",
  "@type": [
    "schema:PropertyValue"
  ],
  "schema:propertyID": [
    {
      "@id": "ada:parameter/module/CalibrationFactor/calibrationFactorAndDeterminationMethod"
    }
  ],
  "schema:name": "Calibration Factor and Determination Method",
  "schema:value": "example value"
}
```

#### jsonld
```jsonld
{
  "@context": [
    {
      "schema": "http://schema.org/"
    },
    "https://amds-ldeo.github.io/geochemBuildingBlocks/_sources/BaseSchema/modules/calibrationFactor/context.jsonld"
  ],
  "@id": "ada:parameter/module/CalibrationFactor/calibrationFactorAndDeterminationMethod",
  "@type": [
    "schema:PropertyValue"
  ],
  "schema:propertyID": [
    {
      "@id": "ada:parameter/module/CalibrationFactor/calibrationFactorAndDeterminationMethod"
    }
  ],
  "schema:name": "Calibration Factor and Determination Method",
  "schema:value": "example value"
}
```

#### ttl
```ttl
@prefix schema1: <http://schema.org/> .

<ada:parameter/module/CalibrationFactor/calibrationFactorAndDeterminationMethod> a schema1:PropertyValue ;
    schema1:name "Calibration Factor and Determination Method" ;
    schema1:propertyID <ada:parameter/module/CalibrationFactor/calibrationFactorAndDeterminationMethod> ;
    schema1:value "example value" .


```


### CalibrationFactor published parameter (Param_Procedure_calibrationFactorAndDeterminationMethod)
A parameter the CalibrationFactor module publishes for the procedure side. The module places no root fields, so its parameters are what it contributes: a consuming TAPP unions this branch into its own additionalProperty anyOf.
#### json
```json
{
  "@id": "ada:parameter/module/CalibrationFactor/calibrationFactorAndDeterminationMethodDefault",
  "@type": [
    "schema:PropertyValueSpecification"
  ],
  "schema:valueName": "calibrationFactorAndDeterminationMethodDefault",
  "schema:name": "Calibration Factor and Determination Method",
  "ada:dataType": "string",
  "ada:fieldScope": "session",
  "schema:readonlyValue": false,
  "ada:tier": "R"
}
```

#### jsonld
```jsonld
{
  "@context": [
    {
      "ada": "https://ada.astromat.org/metadata/",
      "schema": "http://schema.org/"
    },
    "https://amds-ldeo.github.io/geochemBuildingBlocks/_sources/BaseSchema/modules/calibrationFactor/context.jsonld"
  ],
  "@id": "ada:parameter/module/CalibrationFactor/calibrationFactorAndDeterminationMethodDefault",
  "@type": [
    "schema:PropertyValueSpecification"
  ],
  "schema:valueName": "calibrationFactorAndDeterminationMethodDefault",
  "schema:name": "Calibration Factor and Determination Method",
  "ada:dataType": "string",
  "ada:fieldScope": "session",
  "schema:readonlyValue": false,
  "ada:tier": "R"
}
```

#### ttl
```ttl
@prefix ada: <https://ada.astromat.org/metadata/> .
@prefix schema1: <http://schema.org/> .
@prefix xsd: <http://www.w3.org/2001/XMLSchema#> .

<https://ada.astromat.org/metadata/parameter/module/CalibrationFactor/calibrationFactorAndDeterminationMethodDefault> a schema1:PropertyValueSpecification ;
    schema1:name "Calibration Factor and Determination Method" ;
    schema1:readonlyValue false ;
    schema1:valueName "calibrationFactorAndDeterminationMethodDefault" ;
    ada:dataType "string" ;
    ada:fieldScope "session" ;
    ada:tier "R" .


```

## Schema

```yaml
$schema: https://json-schema.org/draft/2020-12/schema
title: 'TAPP Composition Module: CalibrationFactor'
description: "The shared CalibrationFactor block of the 2026-08-11 TAPP library (Claude
  Skills for TAPP/Module_CalibrationFactor.csv), composed by 14 of the sixteen delivery
  tables. 1 owned fields over 0 schema paths.\n\nGENERATED by tools/build_module_bb.py
  from the module's CSV and its schema-path sidecar \u2014 edit those, not this file.
  Requiredness follows the TAPP tier matrix: a field Basic on a side is required there,
  Advanced is permitted, N/A is absent from that side entirely.\n\nThis is a PROFILE,
  not a vocabulary: it asserts which properties a conforming procedure or analysis
  carries. Column F (Example / Allowed Content) is consumer-owned per the module manifest,
  so no enum is pinned here \u2014 a consuming TAPP may narrow a value space, never
  widen it."
$defs:
  Param_Analysis_calibrationFactorAndDeterminationMethod:
    title: Calibration Factor and Determination Method
    description: 'An externally-calibrated factor that converts the measured quantity
      into the reported quantity, how it was determined, and its uncertainty. Applies
      where the conversion depends on a factor calibrated against a reference of independently
      known value, rather than on the instrument response alone. Distinct from the
      fields that name the calibration material and that state which approach applies
      to which analyte, where the technique has them: this field records the resulting
      factor itself.'
    type: object
    properties:
      '@id':
        const: ada:parameter/module/CalibrationFactor/calibrationFactorAndDeterminationMethod
      '@type':
        const:
        - schema:PropertyValue
      schema:propertyID:
        const:
        - '@id': ada:parameter/module/CalibrationFactor/calibrationFactorAndDeterminationMethod
      schema:name:
        const: Calibration Factor and Determination Method
      schema:value:
        type: string
    required:
    - '@id'
    - '@type'
    - schema:propertyID
    - schema:name
    - schema:value
  Param_Procedure_calibrationFactorAndDeterminationMethod:
    title: Calibration Factor and Determination Method
    description: 'An externally-calibrated factor that converts the measured quantity
      into the reported quantity, how it was determined, and its uncertainty. Applies
      where the conversion depends on a factor calibrated against a reference of independently
      known value, rather than on the instrument response alone. Distinct from the
      fields that name the calibration material and that state which approach applies
      to which analyte, where the technique has them: this field records the resulting
      factor itself.'
    type: object
    properties:
      '@id':
        const: ada:parameter/module/CalibrationFactor/calibrationFactorAndDeterminationMethodDefault
      '@type':
        const:
        - schema:PropertyValueSpecification
      schema:valueName:
        const: calibrationFactorAndDeterminationMethodDefault
      schema:name:
        const: Calibration Factor and Determination Method
      ada:dataType:
        const: string
      ada:fieldScope:
        const: session
      schema:readonlyValue:
        const: false
      ada:tier:
        const: R
    required:
    - '@id'
    - '@type'
    - schema:valueName
    - schema:name
    - ada:dataType
    - ada:fieldScope

```

Links to the schema:

* YAML version: [schema.yaml](https://amds-ldeo.github.io/geochemBuildingBlocks/build/annotated/BaseSchema/modules/calibrationFactor/schema.json)
* JSON version: [schema.json](https://amds-ldeo.github.io/geochemBuildingBlocks/build/annotated/BaseSchema/modules/calibrationFactor/schema.yaml)


# JSON-LD Context

```jsonld
None
```

You can find the full JSON-LD context here:
[context.jsonld](https://amds-ldeo.github.io/geochemBuildingBlocks/_sources/BaseSchema/modules/calibrationFactor/context.jsonld)

## Sources

* [TAPP Module CalibrationFactor (2026-08-11 library)](https://github.com/amds-ldeo/geochemBuildingBlocks)
* [ADA Metadata Schema v3](https://github.com/amds-ldeo/metadata)

# For developers

The source code for this Building Block can be found in the following repository:

* URL: [https://github.com/amds-ldeo/geochemBuildingBlocks](https://github.com/amds-ldeo/geochemBuildingBlocks)
* Path: `_sources/BaseSchema/modules/calibrationFactor`

