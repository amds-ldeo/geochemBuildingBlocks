- title: Aggregation composed into an analysis (AnalysisIdentification)
  content: The analysis half of the Aggregation module, with every property populated. Generated from
    the schema, so it tracks the sidecar.
  prefixes:
    schema: http://schema.org/
  snippets:
  - language: json
    code: |-
      {
        "schema:variableMeasured": [
          {
            "schema:name": "Goodness-of-Fit or Dispersion Statistic",
            "schema:value": "example value"
          }
        ]
      }
