@prefix sh:     <http://www.w3.org/ns/shacl#> .
@prefix xsd:    <http://www.w3.org/2001/XMLSchema#> .
@prefix schema: <http://schema.org/> .
@prefix prov:   <http://www.w3.org/ns/prov#> .
@prefix ada:    <https://ada.astromat.org/metadata/> .
@prefix cdifd:  <https://cdif.org/validation/0.1/shacl#> .
@base <https://www.ogc.org/rules/template/> .

# GENERATED by tools/build_module_bb.py. Restates the module's required sets so they are checkable
# in RDF as well as in JSON Schema. Warning severity: the JSON Schema $defs enforce this for
# anything composing the module, and a harvested graph may be a partial view that should not be
# called invalid for lacking a field its source document carried.


cdifd:mCICPMSProcedureIdentificationShape
    a sh:NodeShape ;
    sh:target [
        a sh:SPARQLTarget ;
        sh:select """
            SELECT ?this
            WHERE {
                ?this a <https://ada.astromat.org/metadata/TAPPDefinition> .
            }
        """ ;
    ] ;
    sh:property [
        sh:path ada:internalNormalizationElementAndIsotopeRatio ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "MCICPMS requires ada:internalNormalizationElementAndIsotopeRatio on the procedure." ;
    ] ;
    sh:property [
        sh:path schema:actionProcess ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "MCICPMS requires schema:actionProcess on the procedure." ;
    ] ;
    sh:property [
        sh:path schema:additionalProperty ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "MCICPMS requires schema:additionalProperty on the procedure." ;
    ] ;
    sh:property [
        sh:path schema:instrument ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "MCICPMS requires schema:instrument on the procedure." ;
    ] ;
    sh:message "MCICPMS ProcedureIdentification required properties." ;
    .

cdifd:mCICPMSAnalysisIdentificationShape
    a sh:NodeShape ;
    sh:target [
        a sh:SPARQLTarget ;
        sh:select """
            SELECT ?this
            WHERE {
                ?this a <http://schema.org/Dataset> .
            }
        """ ;
    ] ;
    sh:property [
        sh:path dqv:hasQualityMeasurement ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "MCICPMS requires dqv:hasQualityMeasurement on the analysis." ;
    ] ;
    sh:message "MCICPMS AnalysisIdentification required properties." ;
    .
