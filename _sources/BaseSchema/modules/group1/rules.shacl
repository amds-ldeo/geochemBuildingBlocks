@prefix sh:     <http://www.w3.org/ns/shacl#> .
@prefix xsd:    <http://www.w3.org/2001/XMLSchema#> .
@prefix schema: <http://schema.org/> .
@prefix prov:   <http://www.w3.org/ns/prov#> .
@prefix ada:    <https://ada.astromat.org/metadata/> .
@prefix cdifd:  <https://cdif.org/validation/0.1/shacl#> .
@base <https://www.ogc.org/rules/template/> .

# GENERATED by tools/build_module_bb.py. Restates the module's required sets so they are checkable
# in RDF as well as in JSON Schema. Warning severity: the JSON Schema $defs enforce this for
# anything composing the module, and a harvested graph may be a partial view that should not be
# called invalid for lacking a field its source document carried.


cdifd:group1ProcedureIdentificationShape
    a sh:NodeShape ;
    sh:target [
        a sh:SPARQLTarget ;
        sh:select """
            SELECT ?this
            WHERE {
                ?this a <https://ada.astromat.org/metadata/TAPPDefinition> .
            }
        """ ;
    ] ;
    sh:property [
        sh:path schema:creator ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "Group1 requires schema:creator on the procedure." ;
    ] ;
    sh:property [
        sh:path schema:datePublished ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "Group1 requires schema:datePublished on the procedure." ;
    ] ;
    sh:property [
        sh:path schema:location ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "Group1 requires schema:location on the procedure." ;
    ] ;
    sh:property [
        sh:path schema:measurementTechnique ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "Group1 requires schema:measurementTechnique on the procedure." ;
    ] ;
    sh:property [
        sh:path schema:name ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "Group1 requires schema:name on the procedure." ;
    ] ;
    sh:message "Group1 ProcedureIdentification required properties." ;
    .

cdifd:group1AnalysisIdentificationShape
    a sh:NodeShape ;
    sh:target [
        a sh:SPARQLTarget ;
        sh:select """
            SELECT ?this
            WHERE {
                ?this a <http://schema.org/Dataset> .
            }
        """ ;
    ] ;
    sh:property [
        sh:path prov:wasGeneratedBy ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "Group1 requires prov:wasGeneratedBy on the analysis." ;
    ] ;
    sh:property [
        sh:path schema:contributor ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "Group1 requires schema:contributor on the analysis." ;
    ] ;
    sh:property [
        sh:path schema:funding ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "Group1 requires schema:funding on the analysis." ;
    ] ;
    sh:property [
        sh:path schema:measurementTechnique ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "Group1 requires schema:measurementTechnique on the analysis." ;
    ] ;
    sh:message "Group1 AnalysisIdentification required properties." ;
    .
