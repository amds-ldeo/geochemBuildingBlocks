- title: CollisionCell composed into a procedure (ProcedureIdentification)
  content: The procedure half of the CollisionCell module, with every property populated. Generated from
    the schema, so it tracks the sidecar.
  prefixes:
    schema: http://schema.org/
  snippets:
  - language: json
    code: |-
      {
        "schema:instrument": [
          {
            "schema:additionalType": "example value",
            "schema:hasPart": [
              {
                "schema:additionalType": "example value",
                "schema:name": "example value"
              }
            ]
          }
        ]
      }
