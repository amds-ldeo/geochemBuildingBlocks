- title: SamplingUnitSelection composed into a procedure (ProcedureIdentification)
  content: The procedure half of the SamplingUnitSelection module, with every property populated. Generated
    from the schema, so it tracks the sidecar.
  prefixes:
    ada: https://ada.astromat.org/metadata/
  snippets:
  - language: json
    code: |-
      {
        "ada:samplingUnitSelectionCriteriaDefault": "example value"
      }
