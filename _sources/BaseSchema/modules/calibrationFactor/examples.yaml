- title: CalibrationFactor composed into a procedure (ProcedureIdentification)
  content: The procedure half of the CalibrationFactor module, with every property populated. Generated
    from the schema, so it tracks the sidecar.
  prefixes:
    schema: http://schema.org/
  snippets:
  - language: json
    code: |-
      {
        "schema:variableMeasured": [
          {
            "schema:name": "Calibration Factor and Determination Method",
            "schema:defaultValue": "example value"
          }
        ]
      }
- title: CalibrationFactor composed into an analysis (AnalysisIdentification)
  content: The analysis half of the CalibrationFactor module, with every property populated. Generated
    from the schema, so it tracks the sidecar.
  prefixes:
    schema: http://schema.org/
  snippets:
  - language: json
    code: |-
      {
        "schema:variableMeasured": [
          {
            "schema:name": "Calibration Factor and Determination Method",
            "schema:value": "example value"
          }
        ]
      }
