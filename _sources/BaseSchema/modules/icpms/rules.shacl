@prefix sh:     <http://www.w3.org/ns/shacl#> .
@prefix xsd:    <http://www.w3.org/2001/XMLSchema#> .
@prefix schema: <http://schema.org/> .
@prefix prov:   <http://www.w3.org/ns/prov#> .
@prefix ada:    <https://ada.astromat.org/metadata/> .
@prefix cdifd:  <https://cdif.org/validation/0.1/shacl#> .
@base <https://www.ogc.org/rules/template/> .

# GENERATED by tools/build_module_bb.py. Restates the module's required sets so they are checkable
# in RDF as well as in JSON Schema. Warning severity: the JSON Schema $defs enforce this for
# anything composing the module, and a harvested graph may be a partial view that should not be
# called invalid for lacking a field its source document carried.


cdifd:iCPMSProcedureIdentificationShape
    a sh:NodeShape ;
    sh:target [
        a sh:SPARQLTarget ;
        sh:select """
            SELECT ?this
            WHERE {
                ?this a <https://ada.astromat.org/metadata/TAPPDefinition> .
            }
        """ ;
    ] ;
    sh:property [
        sh:path ada:analysisSequenceDefault ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "ICPMS requires ada:analysisSequenceDefault on the procedure." ;
    ] ;
    sh:property [
        sh:path ada:blankBackgroundCorrectionMethod ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "ICPMS requires ada:blankBackgroundCorrectionMethod on the procedure." ;
    ] ;
    sh:property [
        sh:path ada:calibrationMeasurementFrequency ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "ICPMS requires ada:calibrationMeasurementFrequency on the procedure." ;
    ] ;
    sh:property [
        sh:path ada:internalStandardElement ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "ICPMS requires ada:internalStandardElement on the procedure." ;
    ] ;
    sh:property [
        sh:path ada:ionCounterDeadTimeDefault ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "ICPMS requires ada:ionCounterDeadTimeDefault on the procedure." ;
    ] ;
    sh:property [
        sh:path ada:oxideProductionMethodAndThreshold ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "ICPMS requires ada:oxideProductionMethodAndThreshold on the procedure." ;
    ] ;
    sh:property [
        sh:path ada:signalIntegrationIntervalMethod ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "ICPMS requires ada:signalIntegrationIntervalMethod on the procedure." ;
    ] ;
    sh:property [
        sh:path ada:uncertaintyLevel ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "ICPMS requires ada:uncertaintyLevel on the procedure." ;
    ] ;
    sh:property [
        sh:path schema:instrument ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "ICPMS requires schema:instrument on the procedure." ;
    ] ;
    sh:message "ICPMS ProcedureIdentification required properties." ;
    .

cdifd:iCPMSAnalysisIdentificationShape
    a sh:NodeShape ;
    sh:target [
        a sh:SPARQLTarget ;
        sh:select """
            SELECT ?this
            WHERE {
                ?this a <http://schema.org/Dataset> .
            }
        """ ;
    ] ;
    sh:property [
        sh:path dqv:hasQualityMeasurement ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "ICPMS requires dqv:hasQualityMeasurement on the analysis." ;
    ] ;
    sh:property [
        sh:path schema:variableMeasured ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "ICPMS requires schema:variableMeasured on the analysis." ;
    ] ;
    sh:message "ICPMS AnalysisIdentification required properties." ;
    .
