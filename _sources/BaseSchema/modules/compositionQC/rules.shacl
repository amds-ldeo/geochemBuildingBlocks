@prefix sh:     <http://www.w3.org/ns/shacl#> .
@prefix xsd:    <http://www.w3.org/2001/XMLSchema#> .
@prefix schema: <http://schema.org/> .
@prefix prov:   <http://www.w3.org/ns/prov#> .
@prefix ada:    <https://ada.astromat.org/metadata/> .
@prefix cdifd:  <https://cdif.org/validation/0.1/shacl#> .
@base <https://www.ogc.org/rules/template/> .

# GENERATED by tools/build_module_bb.py. Restates the module's required sets so they are checkable
# in RDF as well as in JSON Schema. Warning severity: the JSON Schema $defs enforce this for
# anything composing the module, and a harvested graph may be a partial view that should not be
# called invalid for lacking a field its source document carried.


cdifd:compositionQCProcedureIdentificationShape
    a sh:NodeShape ;
    sh:target [
        a sh:SPARQLTarget ;
        sh:select """
            SELECT ?this
            WHERE {
                ?this a <https://ada.astromat.org/metadata/TAPPDefinition> .
            }
        """ ;
    ] ;
    sh:property [
        sh:path ada:primaryStandardNameDefault ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "CompositionQC requires ada:primaryStandardNameDefault on the procedure." ;
    ] ;
    sh:property [
        sh:path ada:secondaryReferenceMaterialDefault ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "CompositionQC requires ada:secondaryReferenceMaterialDefault on the procedure." ;
    ] ;
    sh:property [
        sh:path schema:actionProcess ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "CompositionQC requires schema:actionProcess on the procedure." ;
    ] ;
    sh:message "CompositionQC ProcedureIdentification required properties." ;
    .

cdifd:compositionQCAnalysisIdentificationShape
    a sh:NodeShape ;
    sh:target [
        a sh:SPARQLTarget ;
        sh:select """
            SELECT ?this
            WHERE {
                ?this a <http://schema.org/Dataset> .
            }
        """ ;
    ] ;
    sh:property [
        sh:path schema:variableMeasured ;
        sh:minCount 1 ;
        sh:severity sh:Warning ;
        sh:message "CompositionQC requires schema:variableMeasured on the analysis." ;
    ] ;
    sh:message "CompositionQC AnalysisIdentification required properties." ;
    .
